import {Link} from "react-router";
const Settings = () => {
    return (
        <div>
            <h1>Settings</h1>
            <p>This is where settings are gonna be</p>
            <Link to="/">Back to Home</Link>
        </div>
    );
}
export default Settings;

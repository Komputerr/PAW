import {Link} from "react-router";
const Contacts = () => {
    return (
        <div>
            <h1>Contacts</h1>
            <p>This is where contacts are gonna be</p>
            <Link to="/">Back to Home</Link>
        </div>
    );
}
export default Contacts;
